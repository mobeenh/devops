node {
     if (env.BRANCH_NAME == 'master'){
	     stage('build'){
		    echo "This  is master branch"
			}
	}
	    if (env.BRANCH_NAME == 'develop'){
	     stage('build'){
		    echo "This  is develop"
			}
	}
}

